# Diagnostic fixture: low-trust pull requests must not be granted cache writes.
on: pull_request
cache-mode: write

jobs:
  unsafe-cache-writer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/cache@v4
        with:
          path: build-cache
          key: build-${{ github.base_ref }}
